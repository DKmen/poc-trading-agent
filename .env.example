# .env - project environment variables
# Copy this file to .env (already here) and fill the secret values.
# Do NOT commit secrets to Git. Add .env to .gitignore if not already.

# OpenAI / LLM provider
OPENAI_API_KEY=

# Database (example sqlite, change for production)
DATABASE_URL=sqlite:///./data.db

# App configuration
DEBUG=true
PORT=8000

# Alpha Vantage API key
ALPHA_VANTAGE=

LANGSMITH_TRACING="true"
LANGSMITH_ENDPOINT="https://api.smith.langchain.com"
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=
